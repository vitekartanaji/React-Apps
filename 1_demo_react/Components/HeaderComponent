// Simple class component (without state & props)

import React from "react";
class HeaderComponent extends React.Component{
    render(){
        return(
        <p>Tanaji Vitekar</p>
    )}
}

export default HeaderComponent;
